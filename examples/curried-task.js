'use strict'

// via currying, your task can accept multiple parameters
// in functional form, data ought to be passed in as the last parameter
const task = (options = {}) => contents => new Promise((resolve, reject) => {

})

module.exports = task
